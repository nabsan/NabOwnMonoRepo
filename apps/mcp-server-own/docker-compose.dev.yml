version: "3.9"

services:
  mcp-server-own:
    build: .
    container_name: mcp-server-own-dev
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    ports:
      - "8000:8000"
    command: >
      uvicorn api.main:app
      --host 0.0.0.0
      --port 8000
      --reload

